import {
  extractLineEvent,
  extractParams,
  extractType,
  extractUserId,
} from "../../src/lib/jsonParser";

describe("extractLineEvent", () => {
  it("is true", () => {
    expect();
  });
});

describe("extractParams", () => {
  it("is true", () => {
    expect();
  });
});

describe("extractType", () => {
  it("is true", () => {
    expect();
  });
});

describe("extractUserId", () => {
  it("is true", () => {
    expect();
  });
});
